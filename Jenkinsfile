pipeline {
        agent any
        stages {
                stage("Run code"){
                        steps {
                                echo "the branch: my branch"

                }       }

        }
}
